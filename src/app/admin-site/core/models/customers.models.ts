export interface CustomerModel {
  userName: string;
  firstName: string;
  lastName: string;
  email: string;
  gender: string;
}

export interface CustomerData {
  firstName: string;
  lastName: string;
  userName: string;
}
